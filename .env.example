# Copy to .env locally if desired. Never commit real values.
GITHUB_TOKEN=
GITHUB_API_VERSION=2026-03-10
GITHUB_USER_AGENT=eduos-github-radar/0.1

# Future Vercel + managed Postgres deployment. Use the provider's pooled URL
# at runtime and the direct URL only for migrations.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
RADAR_ADMIN_TOKEN=
PUBLIC_SITE_URL=
