# Public support URL. Leave blank until the Buy Me a Coffee creator page exists.
NEXT_PUBLIC_BUY_ME_A_COFFEE_URL=

# Private server-only Postgres connection. Never prefix this with NEXT_PUBLIC.
# Required for the curator-only project suggestion inbox.
DATABASE_URL=
# Used only by scheduled metadata refresh jobs, never in browser code.
GITHUB_TOKEN=
CRON_SECRET=
